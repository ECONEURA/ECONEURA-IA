import express from 'express';
const router = express.Router();
router.get('/', (req, res) => res.json({ message: 'business-intelligence placeholder' }));
export default router;
//# sourceMappingURL=business-intelligence.js.map