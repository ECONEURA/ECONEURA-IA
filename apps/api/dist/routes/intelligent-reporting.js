import express from 'express';
const router = express.Router();
router.get('/', (req, res) => res.json({ message: 'intelligent-reporting placeholder' }));
export default router;
//# sourceMappingURL=intelligent-reporting.js.map